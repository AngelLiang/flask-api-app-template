用户登出
---
tags:
  - auth

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token

responses:
  200:
    description: 用户成功登出
    produces:
      - application/json
    schema:
      $ref: '#/definitions/CommenResponse'

definitions:
  LogoutResponse:
    properties:
      code:
        type: integer
        require: true
        example: 20000
      message:
        type: string
        require: true
        example: Success!
      request:
        type: string
        require: true
        example: '#/host'
      data:
        type: object
        example: {}
