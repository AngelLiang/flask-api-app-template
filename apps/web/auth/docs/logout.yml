用户登出
---
tags:
  - Auth

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token

responses:
  200:
    description: 用户成功登出
    produces:
      - application/json
    schema:
      $ref: '#/definitions/CommenResponse'

definitions:
  LogoutResponse:
    properties:
      code:
        $ref: '#/definitions/code'
      message:
        $ref: '#/definitions/message'
      request:
        $ref: '#/definitions/request'
      data:
        type: object
        example: {}
