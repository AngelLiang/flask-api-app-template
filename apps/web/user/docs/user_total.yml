Get Total User
---
tags:
  - user

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token

responses:
  200:
    description: 请求成功
    schema:
      $ref: '#/definitions/CommenTotalResponse'

definitions:
  CommenTotalResponse:
    type: object
    properties:
      code:
        type: integer
        require: true
        example: 20000
      message:
        type: string
        require: true
        example: 'Success!'
      request:
        type: string
        require: true
        example: '{base_url}'
      data:
        type: object
        properties:
          total:
            type: integer
            require: true
            example: 1
