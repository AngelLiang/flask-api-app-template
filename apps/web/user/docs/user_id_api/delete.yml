Delete User Model by user_id
---
tags:
  - user

consumes:
  - application/json

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token
  - in: path
    name: user_id
    require: true
    default: 1

responses:
  200:
    description: OK
    produces:
      - application/json
    schema:
      type: object
      properties:
        code:
          $ref: '#/definitions/code'
        message:
          $ref: '#/definitions/message'
        request:
          $ref: '#/definitions/request'
