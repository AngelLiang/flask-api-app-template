Get User Model by user_id
---
tags:
  - User

consumes:
  - application/json

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token
  - in: path
    name: user_id
    require: true
    default: 1

responses:
  200:
    description: OK
    produces:
      - application/json
    schema:
      type: object
      properties:
        code:
          $ref: '#/definitions/code'
        message:
          $ref: '#/definitions/message'
        request:
          $ref: '#/definitions/request'
        data:
          type: object
          require: true
          $ref: '#/definitions/UserModel'

definitions:
  UserModel:
    properties:
      id:
        type: integer
        example: 1
      username:
        type: string
        example: admin

      is_active:
        type: boolean
        example: true

      fullname:
        type: string
      email:
        type: string
        example: admin@example.com
      is_email_confirm:
        type: boolean
        example: true
      phone:
        type: string
      is_phone_confirm:
        type: string
        example: true

      create_datetime:
        type: string
        example: yyyy-mm-dd hh:MM:ss
