Change User Model by user_id
---
tags:
  - User

consumes:
  - application/json

parameters:
  - name: Authorization
    in: header
    type: string
    require: true
    description: token
  - in: path
    name: user_id
    require: true
    default: 1
  - in: body
    name: User Information object
    description:
    schema:
      type: object
      properties:
        username:
          type: string
          require: false
          description: username
        password:
          type: string
          require: false
          description: password
        is_active:
          type: boolean
          require: false
          default: false
        phone:
          type: boolean
          require: false
          default: false

responses:
  200:
    description: OK
    produces:
      - application/json
    schema:
      type: object
      properties:
        code:
          $ref: '#/definitions/code'
        message:
          $ref: '#/definitions/message'
        request:
          $ref: '#/definitions/request'
        data:
          type: object
          require: true
          $ref: '#/definitions/UserModel'
